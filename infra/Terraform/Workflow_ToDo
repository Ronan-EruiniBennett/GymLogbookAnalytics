State 1 Frontend
S3 Bucket, bucket policies, and configurations 
Route 53 + ACM
cloudfront with OAC and ACM cert completed
Include Upload object Index.html in static bucket

State 1 Test:
Success

State 2 Pipeline
Cognito COMPLETE
API gateway 
Terraform writes Cognito and API gateway variables into index.html file
Lambda function + upload Lambda application code


State 3 Analytics
create glue db of data bucket
setup athena to query using glue and sending results to query bucket
setup quicksight

Notes for next steps:
Need to add a resource based permission to lambda to allow invocations from api gateway. Retest. Check if lambda is invokeable from api gateway.
add local terraform writes to index.html.
